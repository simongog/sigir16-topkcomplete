This directory contains two sample inputs for your system.

## Input format

Each line of the input file should consist of a (string, weight)-pair.
The string and weight should be separated by a single tab `\t`.
